server {
    listen 80;
    listen [::]:80;
    server_name podvval.xyz admin.podvval.xyz;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
        default_type text/plain;
    }

    location / {
        return 503;
    }
}
